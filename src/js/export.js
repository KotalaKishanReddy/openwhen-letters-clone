/* ═══════════════════════════════════════════
   EXPORT.JS — Serialize canvas → JSON payload
═══════════════════════════════════════════ */

// TODO: serializeProject() → { pages, elements, metadata }
// TODO: deserializeProject(json) → restore editor state
// TODO: exportToPNG(pageEl) — html2canvas
// TODO: exportToPDF() — multi-page jsPDF
// TODO: exportToJSON() — raw project file
// TODO: prepareForEncryption() → Uint8Array

export default {};
