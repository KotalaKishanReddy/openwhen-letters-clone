/* ═══════════════════════════════════════════
   EDITOR.JS — Canvas controller
═══════════════════════════════════════════ */

// TODO: init(canvasEl) — mount canvas, bind events
// TODO: addElement(type, data) — text / photo / sticker / shape
// TODO: selectElement(id)
// TODO: deleteSelected()
// TODO: duplicateSelected()
// TODO: bringForward() / sendBackward()
// TODO: groupSelected() / ungroup()
// TODO: getState() — serialize all elements
// TODO: loadState(json) — restore from saved

export default {};
