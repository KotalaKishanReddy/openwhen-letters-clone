/* ═══════════════════════════════════════════
   VIEWER.JS — Decrypt .scrap, reconstruct spreads
═══════════════════════════════════════════ */

// TODO: init() — show decrypt-gate screen
// TODO: handleFileUpload(file) — read .scrap file
// TODO: attemptDecrypt(file, passphrase)
//   → success: renderViewer(project)
//   → fail: show error shake animation
// TODO: renderViewer(project) — build envelope grid
// TODO: openLetter(pageId) — flip animation → reader
// TODO: closeLetter() — return to grid
// TODO: readOnlyRender(pageData) — reconstruct page elements

export default {};
