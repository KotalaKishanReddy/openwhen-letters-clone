/* ═══════════════════════════════════════════
   DRAG.JS — Pointer drag + resize engine
═══════════════════════════════════════════ */

// TODO: makeDraggable(el, options)
// TODO: makeResizable(el, handles)
// TODO: makeRotatable(el, handle)
// TODO: snapToGrid(x, y, gridSize)
// TODO: constrainToBounds(x, y, w, h, bounds)
// TODO: pointerdown / pointermove / pointerup handlers

export default {};
