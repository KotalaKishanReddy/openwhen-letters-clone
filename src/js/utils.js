/* ═══════════════════════════════════════════
   UTILS.JS — Shared helpers
═══════════════════════════════════════════ */

// TODO: uuid() → crypto.randomUUID()
// TODO: clamp(value, min, max)
// TODO: debounce(fn, ms)
// TODO: throttle(fn, ms)
// TODO: fileToBase64(file) → Promise<string>
// TODO: base64ToUint8Array(b64) → Uint8Array
// TODO: uint8ArrayToBase64(arr) → string
// TODO: downloadFile(blob, filename)
// TODO: deepClone(obj) → structuredClone
// TODO: formatBytes(bytes) → '2.4 MB'

export default {};
